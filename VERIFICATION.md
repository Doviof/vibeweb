# 验收记录

## 本次视觉更新

- 全语言品牌统一 EN ROUTE；默认白底，手动深色偏好继续保留。
- 浏览器实测 Courier Prime 700、12px / 18.6px；桌面导航高 44px。
- 首页改为居中完整大图、居中分类/地点/日期说明，移除侧边文字装饰与原来的绿色色调。
- 三语 × 五组视口（320×568、390×844、768×1024、844×390、1440×960）检查无横向溢出、无导航重叠，说明均在可视区域。
- 归档、随记、关于、详情在 320px 与 1440px 下无横向溢出或损坏图片；归档筛选、列表视图、主题切换、详情返回和大图打开正常。
- 最新效果以 `screenshots/gallery-*-final.png` 为准，之前的 `i18n-*` 和 `desktop-final.png` 是旧版样式。


## 自动检查

- `npm test`：9 项校验通过，包括内容完整性、三种语言的界面文案、插值参数、全部记录/随记译文、图片路径、替代文本与邮箱格式。
- `npm run build`：响应式图像生成、TypeScript 严格检查、Vite 生产打包通过。
- `npm audit --registry=https://registry.npmjs.org`：本次检查未发现已知漏洞。

## 浏览器验证

使用 Playwright MCP 控制 Chromium，对实际页面进行截图与交互检查。

- 首页视口：320×568、390×844、768×1024、844×390、1440×960、1920×1080。
- 主视图、归档、详情、随记、关于在已测尺寸无横向溢出。
- 390×844 额外使用移动视口、3 倍像素密度、触摸模拟；下一则按钮可通过 tap 操作。
- 语言：页面支持中文、English、日本語。语言菜单的鼠标、触摸、键盘箭头、Enter 与 Escape 行为通过；语言会在刷新后保持，且切换不改变当前主视图图片位置。
- 生产版本额外验证：移动触摸模拟中英文、日文切换正常；禁止 localStorage 时仍可切换，非法语言值回退中文，无脚本错误。
- 三语首页各检查 320×568、390×844、768×1024、844×390、1440×960，共 15 组视口：无横向溢出、无导航重叠，图片说明均在可视区域内。
- 横图保持原始比例；竖图在手机上完整显示。主视图下一则、进度条定位、键盘 End、详情返回原记录通过。
- 多语言布局：首页、归档、随记、关于、详情与 404 页面在英文和日文的 320×568 视口内均无横向溢出；英文页面未遗留中文正文，所有图片加载正常。
- 图片查看器打开、缩放、左右键切图、Escape 关闭、复制详情链接通过。
- 归档类别筛选、搜索空状态、重置、网格/列表切换通过。
- 浅色主题刷新后保留，再切回深色正常。
- 不存在的详情路由显示站内 404 页面。
- 系统减少动态效果时，滚动吸附禁用。
- 生产预览检查期间未捕获 pageerror 或 HTTP 400+ 的资源响应。

## 截图

最终截图位于 `screenshots/`：

- `desktop-final.png`：桌面主视图。
- `mobile-final.png`：手机横幅照片。
- `mobile-portrait-photo.png`：手机竖幅照片。
- `landscape-final.png`：横屏视口。
- `archive-1440.png`：桌面归档。
- `notes-390.png`：手机随记。
- `mobile-touch-emulation.png`：移动触摸模拟。
- `i18n-en-390-final.png` / `i18n-ja-390-final.png`：本次英文与日文手机主视图。
- `i18n-zh-1440-final.png` / `i18n-en-1440-final.png` / `i18n-ja-1440-final.png`：本次三语桌面主视图。
- `i18n-ja-touch-final.png`：生产版本日文触摸模拟。

其他较早截图用于调整过程中对照，以带 `final` 名称的截图为准。

## 限制

本次未使用真实 iPhone、Android 手机或 WebKit 浏览器。Chromium 的设备模拟不能完全替代 Safari 动态地址栏、安全区与原生触摸行为验证。正式上线前仍建议用真实手机检查。

尚未向任何托管平台发布，也未注册域名、修改 DNS 或创建云端账号。仓库提供的是可静态部署的成品和配置，不含管理后台。示例图片与文字需在作为真实个人记录上线前替换。
